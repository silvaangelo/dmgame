{
  "name": "deathmatch-arena",
  "version": "1.0.0",
  "description": "Multiplayer deathmatch game",
  "type": "module",
  "private": true,
  "scripts": {
    "dev:go": "cd backend-go && go build -o ../dmgame-server . && cd .. && ./dmgame-server",
    "build:go": "cd backend-go && go build -o ../dmgame-server .",
    "start:go": "./dmgame-server",
    "build:frontend": "node scripts/build-frontend.js"
  },
  "devDependencies": {
    "esbuild": "^0.27.3"
  },
  "engines": {
    "bun": ">=1.2"
  }
}
